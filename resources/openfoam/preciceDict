FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      preciceDict;
}

preciceConfig "$precice_cfg";

participant Fluid;

modules (FSI);

interfaces
{
  Interface1
  {
    mesh              Fluid-Mesh-Nodes;
    patches           ($coupled_patches);
    locations         faceNodes;

    readData
    (
      DisplacementDelta
    );

    writeData
    (
    );
  };

  Interface2
  {
    mesh              Fluid-Mesh-Faces;
    patches           ($coupled_patches);
    locations         faceCenters;

    readData
    (
    );

    writeData
    (
      Force
    );
  };
};

FSI
{
  rho rho [1 -3 0 0 0 0 0] 1000;
}